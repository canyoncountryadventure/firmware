name: Build Water Distance Seeed

on:
  push:
    branches:
      - water-distance-seeed
  workflow_dispatch:

concurrency:
  group: water-distance-seeed
  cancel-in-progress: true

permissions: read-all

jobs:
  build-seeed-water-distance:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout water distance Seeed branch
        uses: actions/checkout@v7
        with:
          ref: water-distance-seeed
          submodules: recursive

      - name: Build Seeed XIAO nRF52840 Kit
        uses: meshtastic/gh-action-firmware@main
        with:
          pio_platform: nrf52840
          pio_env: seeed_xiao_nrf52840_kit
          pio_target: build

      - name: Upload firmware artifacts
        uses: actions/upload-artifact@v7
        with:
          name: water-distance-seeed-${{ github.run_number }}
          overwrite: true
          retention-days: 30
          path: |
            release/*.uf2
            release/*.zip
            release/*.bin
            release/*.elf
            release/*.map
            release/*.mt.json
