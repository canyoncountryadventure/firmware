# ARCHIVED — MX2201 legacy branch name

This branch is retained only for Git history, old links, and the repository's current default-branch pointer.

**Do not use this branch for new work or deployment.**

Current production branch:

```text
hobo-mx2201
```

Stable MX2201 recovery tags remain valid. Production development should continue on `hobo-mx2201`.

> Repository setting still to change: switch the GitHub default branch from `mx2201-integration` to `hobo-mx2201`.
