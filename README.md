# ARCHIVED — MX2001 legacy branch name

This branch is retained only for Git history and old links.

**Do not use this branch for new work or deployment.**

Current production branch:

```text
hobo-mx2001
```

The firmware history on this branch is preserved. The rename to `hobo-mx2001` is naming cleanup only; production development should continue on the clean logger-centric branch name.
